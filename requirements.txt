# Core
discord.py>=2.3.0
requests>=2.31.0
aiohttp>=3.9.0
python-dotenv>=1.0.0

# Calendar
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1
google-api-python-client>=2.100.0

# Email
# (uses stdlib imaplib + smtplib — no extra deps)

# Chromecast
pychromecast>=14.0.0

# Whisper (audio pipeline — optional)
# openai-whisper>=20231117

# Utilities
watchdog>=3.0.0
